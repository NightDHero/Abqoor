# PDF Test Data

This directory is for local development PDF files used to verify the Abqoor importer.

Each PDF should follow the MVP import rule: one page equals one complete question image.

Files in this directory are selected manually through the Admin Import interface.

The importer must never automatically scan this directory.

Nothing in this directory is production content.
